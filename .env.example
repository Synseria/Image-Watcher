# ========================================
# Configuration Image Watcher
# ========================================

# Mode d'exécution (development, production, test)
NODE_ENV=development

# Niveau de log (ERROR, WARN, INFO, DEBUG, TRACE)
LOG_LEVEL=INFO

# ========================================
# OpenAI Configuration (optionnel)
# ========================================

# URL de l'API OpenAI ou compatible
OPENAI_BASE_URL=https://api.openai.com/v1

# Clé API OpenAI
OPENAI_API_KEY=sk-your-api-key-here

# Modèle à utiliser (gpt-4-turbo, gpt-4o-mini, etc.)
OPENAI_MODEL=gpt-4o-mini

# ========================================
# Image Watcher Configuration
# ========================================

# Mode de fonctionnement (NOTIFICATION, DISABLED)
# Si défini, écrase les annotations Kubernetes
# IMAGE_WATCHER_MODE=NOTIFICATION

# Stratégie de mise à jour (ALL, MAJOR, MINOR, PATCH)
# Si défini, écrase les annotations Kubernetes
# IMAGE_WATCHER_STRATEGY=ALL

# Surveiller toutes les apps sans annotation (true = mode Flux, pas d'annotations requises sur les pods)
# IMAGE_WATCHER_WATCH_ALL=true

# Fichier de persistance de l'état des notifications (nécessite un PVC monté sur le répertoire)
# Sans cette variable, l'état est perdu au redémarrage du pod
# IMAGE_WATCHER_STATE_FILE=/data/state.json

# Token pour authentifier les webhooks Flux (optionnel - si absent, endpoint non protégé)
# Si défini, la requête doit fournir ?token=<valeur> dans la query string
# FLUX_WEBHOOK_TOKEN=your-secret-token-here

# Exécuter la vérification au démarrage (TRUE, FALSE)
RUN_ON_BOOT=FALSE

# ========================================
# GitHub Configuration
# ========================================

# Token GitHub pour éviter les limites de rate limit
# Créer un token sur : https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# ========================================
# Notification Configuration (optionnel)
# ========================================

# Discord Webhook URL
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Telegram Bot Token
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# TELEGRAM_CHAT_ID=-1001234567890

# ========================================
# Kubernetes Configuration
# ========================================

# Si vous utilisez un kubeconfig custom
# KUBECONFIG=/path/to/kubeconfig

# Namespace par défaut (optionnel)
# KUBERNETES_NAMESPACE=default
