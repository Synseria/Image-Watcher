# 🎨 Diagrammes et visualisations

Ce document contient des représentations visuelles du workflow CI/CD.

---

## 📊 Architecture de la pipeline

```
┌─────────────────────────────────────────────────────────────────────┐
│                        GitHub Actions Pipeline                       │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│  Push Code   │
│  or Tag      │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────────┐
│                         Trigger Events                                │
├──────────────────────────────────────────────────────────────────────┤
│  • Push to any branch                                                 │
│  • Pull Request to master/dev                                       │
│  • Push tag v*                                                        │
└──────────────────────┬───────────────────────────────────────────────┘
                       │
       ┌───────────────┼───────────────┐
       ▼               ▼               ▼
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│   Tests     │ │    Build    │ │   Docker    │
│             │ │             │ │             │
│ ✓ Lint      │ │ ✓ TypeScript│ │ ✓ Multi-tag │
│ ✓ Unit Test │ │ ✓ Artifacts │ │ ✓ GHCR Push │
└──────┬──────┘ └──────┬──────┘ └──────┬──────┘
       │               │               │
       └───────────────┼───────────────┘
                       │
                       ▼
            ┌────────────────────┐
            │   Conditionals     │
            ├────────────────────┤
            │ Branch = master?     │
            │ Branch = dev?  │
            │ Tag = v*?          │
            └─────────┬──────────┘
                      │
         ┌────────────┼────────────┐
         ▼                         ▼
  ┌─────────────┐          ┌─────────────┐
  │   Release   │          │  Notify     │
  │             │          │             │
  │ ✓ GitHub    │          │ ✓ Status    │
  │ ✓ Notes     │          │ ✓ Summary   │
  └─────────────┘          └─────────────┘
```

---

## 🌳 Stratégie de branches

```
master (production) ─────────────────────────────────┬─────────► v1.0.0
    ↑                                              │             │
    │ PR (merge)                                   │             │
    │                                              │          Tag Push
    │                                              │             │
dev (dev) ──────────────────┬─────────────────┘             │
    ↑                           │                               │
    │ PR (merge)                │                               │
    │                           │                               ▼
feature/xxx ───────────────────┘                    ┌──────────────────┐
                                                     │ GitHub Release   │
                                                     │                  │
Actions:                                             │ • Release Notes  │
feature/* : Tests only                               │ • Docker Images  │
dev   : Tests + Build + Docker                   │ • Changelog      │
master      : Tests + Build + Docker                   └──────────────────┘
v*        : Tests + Docker + Release
```

---

## 🐳 Tagging Docker automatique

```
┌─────────────────────────────────────────────────────────────────┐
│                     Docker Image Tags                            │
└─────────────────────────────────────────────────────────────────┘

Push sur dev:
    └─► ghcr.io/synseria/image-watcher:dev
    └─► ghcr.io/synseria/image-watcher:dev-sha-abc123

Push sur master:
    └─► ghcr.io/synseria/image-watcher:master
    └─► ghcr.io/synseria/image-watcher:latest
    └─► ghcr.io/synseria/image-watcher:master-sha-def456

Push tag v1.2.3:
    └─► ghcr.io/synseria/image-watcher:1.2.3  (version exacte)
    └─► ghcr.io/synseria/image-watcher:1.2    (version mineure)
    └─► ghcr.io/synseria/image-watcher:1      (version majeure)
    └─► ghcr.io/synseria/image-watcher:latest (dernière version)
```

---

## 🔄 Workflow développement complet

```
┌─────────────────────────────────────────────────────────────────┐
│                  Cycle de développement                          │
└─────────────────────────────────────────────────────────────────┘

1. Créer une feature
   ┌──────────────────────────────┐
   │ git checkout -b feature/xxx  │
   │ ... développement ...        │
   │ git push origin feature/xxx  │
   └──────────────┬───────────────┘
                  │
                  ▼
         ┌────────────────┐
         │ GitHub Actions │
         │   Tests Only   │
         └────────┬───────┘
                  │
                  ▼
   ┌──────────────────────────────┐
   │  Pull Request → dev      │
   │  ✓ Review                    │
   │  ✓ Tests pass                │
   │  ✓ Merge                     │
   └──────────────┬───────────────┘
                  │
                  ▼
         ┌────────────────┐
         │ GitHub Actions │
         │ Tests + Docker │
         └────────┬───────┘
                  │
                  ▼
   ┌──────────────────────────────┐
   │ Image disponible sur GHCR    │
   │ :dev                     │
   └──────────────┬───────────────┘
                  │
                  ▼
   ┌──────────────────────────────┐
   │  Pull Request → master         │
   │  ✓ Review finale             │
   │  ✓ Tests pass                │
   │  ✓ Merge                     │
   └──────────────┬───────────────┘
                  │
                  ▼
         ┌────────────────┐
         │ GitHub Actions │
         │ Tests + Docker │
         └────────┬───────┘
                  │
                  ▼
   ┌──────────────────────────────┐
   │ Image disponible sur GHCR    │
   │ :latest                      │
   └──────────────┬───────────────┘
                  │
                  ▼
   ┌──────────────────────────────┐
   │ git tag v1.0.0               │
   │ git push origin v1.0.0       │
   └──────────────┬───────────────┘
                  │
                  ▼
         ┌────────────────┐
         │ GitHub Actions │
         │ Full Pipeline  │
         └────────┬───────┘
                  │
                  ▼
   ┌──────────────────────────────┐
   │ ✅ GitHub Release créée      │
   │ ✅ Images :1.0.0, :1.0, :1   │
   │ ✅ Release notes auto        │
   └──────────────────────────────┘

```

---

## 🎯 Décision de jobs (flowchart)

```
                    ┌─────────────┐
                    │   Trigger   │
                    └──────┬──────┘
                           │
                           ▼
                    ┌─────────────┐
                    │    Tests    │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │ Tests Pass? │
                    └──────┬──────┘
                           │
            ┌──────────────┼──────────────┐
            ▼ NO                           ▼ YES
    ┌──────────────┐              ┌──────────────┐
    │   ❌ FAIL    │              │  Continue    │
    │   Pipeline   │              └──────┬───────┘
    │   Stops      │                     │
    └──────────────┘      ┌──────────────┼──────────────┐
                          ▼                             ▼
                   ┌─────────────┐            ┌─────────────┐
                   │ Branch =    │            │ Branch =    │
                   │ master/dev?   │            │ feature/*?  │
                   └──────┬──────┘            └──────┬──────┘
                          │                          │
                 YES ◄────┘                          └────► YES
                    │                                       │
                    ▼                                       ▼
            ┌──────────────┐                     ┌──────────────┐
            │    Build     │                     │   ✅ DONE    │
            └──────┬───────┘                     └──────────────┘
                   │
                   ▼
            ┌──────────────┐
            │   Docker     │
            └──────┬───────┘
                   │
          ┌────────┼────────┐
          ▼                 ▼
   ┌─────────────┐   ┌─────────────┐
   │   Tag v*?   │   │  ✅ DONE    │
   └──────┬──────┘   └─────────────┘
          │
     YES ◄┘
          │
          ▼
   ┌─────────────┐
   │   Release   │
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐
   │  ✅ DONE    │
   └─────────────┘
```

---

## 📦 États des images Docker

```
┌────────────────────────────────────────────────────────────┐
│              Cycle de vie d'une image Docker                │
└────────────────────────────────────────────────────────────┘

Feature Branch
    │
    ▼
┌──────────────┐
│ No Image     │  ← Pas d'image créée (seulement tests)
└──────────────┘

Merge to dev
    │
    ▼
┌──────────────────────────────────────┐
│ ghcr.io/.../image-watcher:dev    │  ← Image de dev
│ ✓ Latest code from dev           │
│ ✓ Peut être instable                 │
│ ✓ Pour tests d'intégration           │
└──────────────────────────────────────┘

Merge to master
    │
    ▼
┌──────────────────────────────────────┐
│ ghcr.io/.../image-watcher:latest     │  ← Image stable
│ ✓ Code testé et approuvé             │
│ ✓ Prêt pour production               │
│ ✓ Tag :master aussi disponible         │
└──────────────────────────────────────┘

Tag release
    │
    ▼
┌──────────────────────────────────────┐
│ ghcr.io/.../image-watcher:1.2.3      │  ← Version officielle
│ ghcr.io/.../image-watcher:1.2        │
│ ghcr.io/.../image-watcher:1          │
│ ✓ Version sémantique                 │
│ ✓ Immuable (ne change jamais)        │
│ ✓ Recommandé pour production         │
└──────────────────────────────────────┘
```

---

## 🔐 Permissions et accès

```
┌────────────────────────────────────────────────────────────┐
│                   GitHub Permissions                        │
└────────────────────────────────────────────────────────────┘

GitHub Actions (GITHUB_TOKEN)
    │
    ├─► contents: read     → Lire le code source
    ├─► contents: write    → Créer des releases
    ├─► packages: write    → Push vers GHCR
    └─► pull-requests: write → Commenter les PRs

GHCR (GitHub Container Registry)
    │
    ├─► Private (défaut)   → Authentification requise
    │   └─► docker login ghcr.io -u USERNAME --password-stdin
    │
    └─► Public (optionnel) → Accessible sans auth
        └─► docker pull ghcr.io/synseria/image-watcher:latest
```

---

## 📈 Timeline d'exécution typique

```
Push to dev
    │
    ├─ 0:00 → Trigger workflow
    │
    ├─ 0:01 → Job: Tests
    │   ├─ Checkout code
    │   ├─ Setup Node.js
    │   ├─ Install deps (cache: ~30s)
    │   ├─ Lint (~10s)
    │   └─ Tests (~20s)
    │
    ├─ 1:00 → Job: Build (parallel)
    │   ├─ Checkout code
    │   ├─ Setup Node.js
    │   ├─ Install deps (cache: ~30s)
    │   └─ Build (~30s)
    │
    ├─ 1:00 → Job: Docker (parallel)
    │   ├─ Checkout code
    │   ├─ Setup Buildx
    │   ├─ Login GHCR
    │   ├─ Extract metadata
    │   └─ Build & Push (~2-3 min with cache)
    │
    └─ 4:00 → ✅ Pipeline complete

Total: ~4 minutes (avec cache)
       ~8 minutes (sans cache, premier build)
```

---

Utilisez ces diagrammes comme référence pour comprendre le flux de travail ! 🚀
